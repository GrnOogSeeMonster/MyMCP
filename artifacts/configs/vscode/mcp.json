{
  "servers": {
    "mcp-core-agent": {
      "type": "stdio",
      "command": "node",
      "args": [
        "./servers/core/dist/index.js"
      ]
    },
    "mcp-aws-helper": {
      "type": "stdio",
      "command": "python",
      "args": [
        "-m",
        "mcp_aws_helper"
      ]
    },
    "mcp-local-knowledge": {
      "type": "http",
      "command": "node",
      "args": [
        "./servers/knowledge/dist/index.js",
        "--port",
        "8001"
      ]
    }
  },
  "inputs": [
    { "name": "MCP_API_BASE", "description": "Set via environment or secret store" },
    { "name": "MCP_TOKEN", "description": "Set via environment or secret store" },
    { "name": "AWS_REGION", "description": "Set via environment or secret store" },
    { "name": "AWS_PROFILE", "description": "Set via environment or secret store" }
  ]
}


